# Environment variable configuration for the backend.
# Copy this file to `.env` and replace the placeholder with your actual API key.
# The `.env` file is used by dotenv to load variables into process.env.

OPENAI_API_KEY=YOUR_API_KEY_HERE
